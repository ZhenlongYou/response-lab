# ResponseLab 频响补偿算法原理说明书

## 1. 目标

在线性时不变假设下，参考链路与待补偿链路的脉冲频响分别为
\(H_r(f)\) 与 \(H_d(f)\)。目标补偿为：

\[
C(f)=\frac{H_r(f)}{H_d(f)}.
\]

## 2. 从脉冲得到频响

若采样间隔为 \(\Delta t\)，则 \(f_s=1/\Delta t\)，并使用：

\[
H(f_k)\approx\Delta t\sum_{n=0}^{N-1}h[n]e^{-j2\pi f_kn\Delta t}.
\]

两份脉冲分别计算后插值到公共物理频率轴，因此允许两者点数和采样率不同。

默认自动频带先把两份幅度谱按各自峰值归一化，选择两者共同不低于 -20 dB
的最长连续区间，再从中留边界裕量得到补偿频带和去斜拟合频带。它只是幅度谱候选，
不是噪声底或相位可信度估计；输入需已去基线，用户必须检查拟合带内相位质量。推荐与
实际分析使用同一公共频率轴，不把公共 Nyquist 当成有效带宽。

## 3. 幅度补偿

补偿频带核心内直接使用：

\[
A(f)=\frac{|H_r(f)|}{|H_d(f)|}.
\]

算法不设置最大补偿增益或衰减，也不通过正则化改变分析结果。若幅度差为 40 dB，实际
补偿就是 40 dB。用户通过选择补偿频带避开不希望反演的区域；数值为零、无法计算响应
比的频点会触发明确错误。

所选频带内完整应用该响应比，频带外严格使用单位响应。仅幅频模式下参考响应为零时，
补偿幅度为零；待补偿响应为零时比值发散，工具会明确报错。

## 4. 相位补偿与时延

相位差为：

\[
\Delta\phi(f)=\phi_r(f)-\phi_d(f).
\]

在去斜拟合频带对每个相位岛拟合 \(\Delta\phi_i(f)\approx mf+b_i\)，估计相对时延
\(\hat\tau=m/(2\pi)\)。实际补偿相位使用：

\[
\phi_\mathrm{used}(f)=\Delta\phi(f)-mf.
\]

斜率使用频谱置信度加权的最小二乘估计。可信频带被零点分成多个相位岛时，每个岛使用
独立截距，斜率只由各岛内部的相位变化共同决定，因此相位展开造成的整数个 \(2\pi\)
岛间偏移不会污染时延估计。零权重频点不参加拟合，非零权重保持原始相对比例。界面和
响应 CSV 中的线性时延分量记录实际移除的 \(mf\)，不会把多个 \(b_i\) 平均成虚假的
全局截距。

因此拟合脉冲的相对时延只显示，不平移待补偿信号；常数相位和非线性相位仍被补偿。
“频率响应”页为了可读性会给每个连续相位岛统一减去整数个 2π，使等价的 360° 显示
为 0° 附近，同时保持岛内连续。“频响差异比较”页使用实际补偿相位的岛级归零结果；
CSV 同时导出原始展开相位差和去时延/岛级归零相位。

## 5. 直接频域应用

待补偿数据先在首尾各镜像延拓一份记录，补偿响应插值到延拓记录的 DFT 频点后直接执行：

\[
X[k]=\operatorname{rfft}\{x[n]\},\qquad
Y[k]=X[k]C(f_k),\qquad
y[n]=\operatorname{irfft}\{Y[k]\}.
\]

这里没有额外的滤波器设计步骤，也没有对分析得到的频响差异做二次近似。反变换后取回
中间原记录，避免循环回卷；输出与输入保持相同点数、时间轴和通道数。
